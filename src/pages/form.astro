---
// Astro component for 2-step contact form
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Form - Señor de los Taxes </title>
    <link rel="icon" type="image/svg+xml" href="/logo.png">
    <style>
        :root {
            --primary-color: #2563eb;
            --primary-hover: #1d4ed8;
            --secondary-color: #64748b;
            --success-color: #10b981;
            --error-color: #ef4444;
            --warning-color: #f59e0b;
            --bg-color: #ffffff;
            --surface-color: #f8fafc;
            --text-primary: #1e293b;
            --text-secondary: #64748b;
            --border-color: #e2e8f0;
            --shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);
            --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
        }

        [data-theme="dark"] {
            --bg-color: #0f172a;
            --surface-color: #1e293b;
            --text-primary: #f1f5f9;
            --text-secondary: #94a3b8;
            --border-color: #334155;
            --shadow: 0 1px 3px 0 rgb(0 0 0 / 0.3), 0 1px 2px -1px rgb(0 0 0 / 0.3);
            --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.3), 0 4px 6px -4px rgb(0 0 0 / 0.3);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-primary);
            line-height: 1.6;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 10px 20px;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: flex-start;
            padding-top: 20px;
        }

        .header {
            text-align: center;
            margin-bottom: 1rem;
        }

        .logo {
            width: 200px;
            height: auto;
            margin: 0 auto 1rem auto;
            display: block;
            transition: filter 0.3s ease;
            filter: brightness(0);
        }

        [data-theme="dark"] .logo {
            filter: brightness(0) invert(1);
        }

        .controls {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-bottom: 0.5rem;
            margin-top: 0;
            z-index: 1000;
        }

        .dark-mode-toggle, .language-toggle {
            background: var(--surface-color);
            border: 1px solid var(--border-color);
            border-radius: 50%;
            width: 50px;
            height: 50px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }

        .language-toggle {
            font-size: 12px;
            font-weight: bold;
            color: var(--text-primary);
        }

        .dark-mode-toggle:hover, .language-toggle:hover {
            transform: scale(1.1);
            box-shadow: var(--shadow-lg);
        }

        .theme-icon {
            width: 24px;
            height: 24px;
            transition: opacity 0.3s ease;
        }

        .form-container {
            background: var(--surface-color);
            border-radius: 16px;
            padding: 2rem;
            box-shadow: var(--shadow-lg);
            border: 1px solid var(--border-color);
            transition: all 0.3s ease;
        }

        .progress-container {
            margin-bottom: 2rem;
        }

        .progress-bar {
            width: 100%;
            height: 8px;
            background-color: var(--border-color);
            border-radius: 4px;
            overflow: hidden;
            margin-bottom: 0.5rem;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, var(--primary-color), var(--primary-hover));
            border-radius: 4px;
            transition: width 0.5s ease;
        }

        .progress-text {
            text-align: center;
            font-size: 0.875rem;
            color: var(--text-secondary);
            font-weight: 500;
        }

        .step {
            display: none;
        }

        .step.active {
            display: block;
        }

        .step-title {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--text-primary);
            margin-bottom: 0.5rem;
            text-align: center;
        }

        .step-description {
            color: var(--text-secondary);
            text-align: center;
            margin-bottom: 2rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-label {
            display: block;
            font-weight: 600;
            color: var(--text-primary);
            margin-bottom: 0.5rem;
            font-size: 0.875rem;
        }

        .form-input {
            width: 100%;
            padding: 0.75rem 1rem;
            border: 2px solid var(--border-color);
            border-radius: 8px;
            font-size: 1rem;
            background-color: var(--bg-color);
            color: var(--text-primary);
            transition: all 0.3s ease;
        }

        .form-input:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgb(37 99 235 / 0.1);
        }

        .form-input.error {
            border-color: var(--error-color);
        }

        .error-message {
            color: var(--error-color);
            font-size: 0.875rem;
            margin-top: 0.5rem;
            display: none;
        }

        .error-message.show {
            display: block;
        }

        .consent-group {
            display: flex;
            flex-direction: column;
            gap: 0.75rem;
            margin: 1.5rem 0 1rem;
        }

        .consent-item {
            display: flex;
            gap: 0.75rem;
            align-items: flex-start;
            font-size: 0.875rem;
            color: var(--text-secondary);
        }

        .consent-input {
            margin-top: 2px;
            height: 18px;
            width: 18px;
            accent-color: var(--primary-color);
        }

        .consent-label {
            color: var(--text-primary);
        }

        .consent-link {
            color: var(--primary-color);
            background: none;
            border: none;
            padding: 0;
            font: inherit;
            cursor: pointer;
            text-decoration: underline;
        }

        .btn {
            width: 100%;
            padding: 0.875rem 1.5rem;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary-color), var(--primary-hover));
            color: white;
        }

        .btn-primary:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
        }

        .btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
        }

        .loading-spinner {
            width: 20px;
            height: 20px;
            border: 2px solid transparent;
            border-top: 2px solid currentColor;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            display: none;
        }

        .loading .loading-spinner {
            display: block;
        }

        .loading .btn-text {
            display: none;
        }

        @keyframes spin {
            to {
                transform: rotate(360deg);
            }
        }

        /* Success UI for submit */
        .success-message {
            display: none;
            margin-top: 0.75rem;
            color: var(--success-color);
            font-weight: 600;
            background: color-mix(in srgb, var(--success-color) 15%, transparent);
            border: 1px solid var(--success-color);
            border-radius: 8px;
            padding: 0.75rem 1rem;
            display: none;
            align-items: center;
            gap: 0.5rem;
            animation: fadeIn 250ms ease-in;
        }

        .success-message.show {
            display: flex;
        }

        .success-icon {
            width: 20px;
            height: 20px;
            color: var(--success-color);
        }

        .success-message.show .success-icon {
            animation: successPop 400ms ease;
        }

        .success-check {
            stroke-dasharray: 40;
            stroke-dashoffset: 40;
            animation: successDraw 500ms ease forwards;
        }

        .submit-error {
            display: none;
            margin-top: 0.75rem;
            color: var(--error-color);
            font-weight: 600;
            background: color-mix(in srgb, var(--error-color) 12%, transparent);
            border: 1px solid var(--error-color);
            border-radius: 8px;
            padding: 0.75rem 1rem;
            align-items: center;
            gap: 0.5rem;
            animation: fadeIn 250ms ease-in;
        }

        .submit-error.show {
            display: flex;
        }

        .error-icon {
            width: 20px;
            height: 20px;
            color: var(--error-color);
        }

        .modal {
            position: fixed;
            inset: 0;
            display: none;
            align-items: center;
            justify-content: center;
            padding: 1.5rem;
            background: rgba(15, 23, 42, 0.55);
            backdrop-filter: blur(6px);
            z-index: 2000;
        }

        .modal.show {
            display: flex;
        }

        .modal-content {
            width: min(640px, 100%);
            background: var(--surface-color);
            color: var(--text-primary);
            border-radius: 16px;
            border: 1px solid var(--border-color);
            box-shadow: var(--shadow-lg);
            padding: 1.5rem;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .modal-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 1rem;
        }

        .modal-title {
            font-size: 1.25rem;
            font-weight: 700;
        }

        .modal-close {
            background: none;
            border: 1px solid var(--border-color);
            border-radius: 999px;
            width: 36px;
            height: 36px;
            color: var(--text-primary);
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }

        .modal-body {
            color: var(--text-secondary);
            font-size: 0.9rem;
            line-height: 1.6;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(4px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes successDraw {
            to { stroke-dashoffset: 0; }
        }

        @keyframes successPop {
            0% { transform: scale(0.85); opacity: 0.6; }
            100% { transform: scale(1); opacity: 1; }
        }

        .thank-you {
            text-align: center;
        }

        .thank-you-icon {
            width: 80px;
            height: 80px;
            margin: 0 auto 1.5rem;
            background: var(--success-color);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .checkmark {
            width: 40px;
            height: 40px;
            stroke: white;
            stroke-width: 3;
            fill: none;
        }

        .thank-you-title {
            font-size: 1.75rem;
            font-weight: 700;
            color: var(--text-primary);
            margin-bottom: 1rem;
        }

        .thank-you-message {
            color: var(--text-secondary);
            font-size: 1.125rem;
            margin-bottom: 2rem;
        }

        .contact-info {
            background: var(--bg-color);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            padding: 1.5rem;
            margin-top: 1rem;
        }

        .contact-info h3 {
            color: var(--text-primary);
            margin-bottom: 1rem;
            font-size: 1.125rem;
        }

        .contact-detail {
            display: flex;
            justify-content: space-between;
            margin-bottom: 0.5rem;
            padding: 0.5rem 0;
            border-bottom: 1px solid var(--border-color);
        }

        .contact-detail:last-child {
            border-bottom: none;
            margin-bottom: 0;
        }

        .contact-label {
            font-weight: 600;
            color: var(--text-secondary);
        }

        .contact-value {
            color: var(--text-primary);
        }

        .data-protection-notice {
            background: linear-gradient(135deg, #f0f9ff, #e0f2fe);
            border: 1px solid #0ea5e9;
            border-radius: 12px;
            padding: 1rem;
            margin: 1.5rem 0;
            display: flex;
            align-items: flex-start;
            gap: 0.75rem;
            transition: all 0.3s ease;
        }

        [data-theme="dark"] .data-protection-notice {
            background: linear-gradient(135deg, #0f172a, #1e293b);
            border-color: #0ea5e9;
        }

        .protection-icon {
            flex-shrink: 0;
            width: 24px;
            height: 24px;
            color: #0ea5e9;
            margin-top: 2px;
        }

        .protection-icon svg {
            width: 100%;
            height: 100%;
        }

        .protection-text {
            font-size: 0.875rem;
            color: var(--text-secondary);
            line-height: 1.5;
            margin: 0;
        }

        @media (max-width: 640px) {
            .container {
                padding: 1rem;
            }

            .form-container {
                padding: 1.5rem;
            }

            .step-title {
                font-size: 1.25rem;
            }

            .controls {
                top: 15px;
                right: 15px;
            }

            .dark-mode-toggle, .language-toggle {
                width: 45px;
                height: 45px;
            }

            .theme-icon {
                width: 20px;
                height: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="controls">
                <button class="language-toggle" id="languageToggle" aria-label="Cambiar idioma">
                    EN
                </button>
                <button class="dark-mode-toggle" id="darkModeToggle" aria-label="Alternar modo oscuro">
                    <svg class="theme-icon sun-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                        <circle cx="12" cy="12" r="5"></circle>
                        <path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"></path>
                    </svg>
                    <svg class="theme-icon moon-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" style="display: none;">
                        <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
                    </svg>
                </button>
            </div>
        </div>

        <div class="form-container">
            <div class="progress-container">
                <div class="progress-bar">
                    <div class="progress-fill" id="progressFill"></div>
                </div>
                <div class="progress-text" id="progressText">Paso 1 de 2</div>
            </div>

            <img src="/logo.png" alt="Logo" class="logo">

            <!-- Step 1: Contact Information -->
            <div class="step active" id="step1">
                <h2 class="step-title" data-es="Información de Contacto" data-en="Contact Information">Información de Contacto</h2>
                <p class="step-description" data-es="Por favor proporciona tus datos de contacto para que podamos comunicarnos contigo." data-en="Please provide your contact details so we can get in touch with you.">Por favor proporciona tus datos de contacto para que podamos comunicarnos contigo.</p>
                
                <form id="contactForm">
                    <div class="form-group">
                        <label class="form-label" for="fullName" data-es="Nombre Completo *" data-en="Full Name *">Nombre Completo *</label>
                        <input type="text" id="fullName" name="name" class="form-input" required>
                        <div class="error-message" id="nameError" data-es="Por favor ingresa tu nombre completo" data-en="Please enter your full name">Por favor ingresa tu nombre completo</div>
                    </div>

                    <div class="form-group">
                        <label class="form-label" for="phone" data-es="Número de Teléfono *" data-en="Phone Number *">Número de Teléfono *</label>
                        <input type="tel" id="phone" name="phone" class="form-input" placeholder="(555) 123-4567" required>
                        <div class="error-message" id="phoneError" data-es="Por favor ingresa un número de teléfono válido" data-en="Please enter a valid phone number">Por favor ingresa un número de teléfono válido</div>
                    </div>

                    <!-- Data Protection Notice -->
                    <div class="data-protection-notice">
                        <div class="protection-icon">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                                <path d="M9 12l2 2 4-4"></path>
                            </svg>
                        </div>
                        <p class="protection-text" data-es="🔒 Tus datos están protegidos y seguros. Utilizamos encriptación SSL y no compartimos tu información con terceros." data-en="🔒 Your data is protected and secure. We use SSL encryption and do not share your information with third parties.">🔒 Tus datos están protegidos y seguros. Utilizamos encriptación SSL y no compartimos tu información con terceros.</p>
                    </div>

                    <div class="consent-group">
                        <label class="consent-item">
                            <input id="privacyConsent" class="consent-input" type="checkbox" required />
                            <span class="consent-label" data-es="Acepto los" data-en="I accept the">Acepto los</span>
                            <button class="consent-link" type="button" data-modal-open="termsModal" data-es="Términos y Condiciones" data-en="Terms & Conditions">Términos y Condiciones</button>
                            <span class="consent-label" data-es="y la" data-en="and the">y la</span>
                            <button class="consent-link" type="button" data-modal-open="privacyModal" data-es="Política de Privacidad" data-en="Privacy Policy">Política de Privacidad</button>
                        </label>
                        <label class="consent-item">
                            <input id="updatesConsent" class="consent-input" type="checkbox" checked />
                            <span class="consent-label" data-es="Quiero recibir novedades y actualizaciones por correo." data-en="I want to receive news and updates by email.">Quiero recibir novedades y actualizaciones por correo.</span>
                        </label>
                        <div class="error-message" id="consentError" data-es="Debes aceptar los términos y la política para continuar." data-en="You must accept the terms and privacy policy to continue.">Debes aceptar los términos y la política para continuar.</div>
                    </div>

                    <button type="submit" class="btn btn-primary" id="submitBtn">
                        <div class="loading-spinner"></div>
                        <span class="btn-text" data-es="Enviar" data-en="Submit">Enviar</span>
                    </button>
                    
                    <!-- Success message after submit -->
                    <div id="submitSuccess" class="success-message" aria-live="polite">
                        <svg class="success-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
                            <polyline class="success-check" points="20,6 9,17 4,12"></polyline>
                        </svg>
                        <span id="submitSuccessText" data-es="Enviado correctamente" data-en="Submitted successfully">Enviado correctamente</span>
                    </div>
                    <div id="submitError" class="submit-error" role="alert" aria-live="polite">
                        <svg class="error-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <circle cx="12" cy="12" r="9"></circle>
                            <path d="M12 7v6"></path>
                            <path d="M12 17h.01"></path>
                        </svg>
                        <span id="submitErrorText"></span>
                    </div>
                </form>
            </div>

            <!-- Step 2: Thank You -->
            <div class="step" id="step2">
                <div class="thank-you">
                    <div class="thank-you-icon">
                        <svg class="checkmark" viewBox="0 0 24 24">
                            <polyline points="20,6 9,17 4,12"></polyline>
                        </svg>
                    </div>
                    <h2 class="thank-you-title" data-es="¡Gracias!" data-en="Thank You!">¡Gracias!</h2>
                    <p class="thank-you-message" data-es="Tu información ha sido enviada exitosamente. Nos pondremos en contacto contigo pronto." data-en="Your information has been successfully submitted. We'll be in touch with you soon.">Tu información ha sido enviada exitosamente. Nos pondremos en contacto contigo pronto.</p>
                    
                    <div class="contact-info">
                        <h3 data-es="Tu Información" data-en="Your Information">Tu Información</h3>
                        <div class="contact-detail">
                            <span class="contact-label" data-es="Nombre:" data-en="Name:">Nombre:</span>
                            <span class="contact-value" id="submittedName">-</span>
                        </div>
                        <div class="contact-detail">
                            <span class="contact-label" data-es="Teléfono:" data-en="Phone:">Teléfono:</span>
                            <span class="contact-value" id="submittedPhone">-</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div id="privacyModal" class="modal" aria-hidden="true">
        <div class="modal-content" role="dialog" aria-modal="true" aria-labelledby="privacyTitle">
            <div class="modal-header">
                <h3 id="privacyTitle" class="modal-title" data-es="Política de Privacidad" data-en="Privacy Policy">Política de Privacidad</h3>
                <button class="modal-close" type="button" data-modal-close aria-label="Close">
                    ✕
                </button>
            </div>
            <div class="modal-body">
                <p data-es="Recopilamos tu nombre y teléfono para contactarte con información sobre nuestros servicios. No compartimos tus datos con terceros sin tu consentimiento." data-en="We collect your name and phone number to contact you about our services. We do not share your data with third parties without your consent.">Recopilamos tu nombre y teléfono para contactarte con información sobre nuestros servicios. No compartimos tus datos con terceros sin tu consentimiento.</p>
                <p data-es="Puedes solicitar acceso, actualización o eliminación de tus datos escribiendo a soporte." data-en="You can request access, updates, or deletion of your data by contacting support.">Puedes solicitar acceso, actualización o eliminación de tus datos escribiendo a soporte.</p>
            </div>
        </div>
    </div>

    <div id="termsModal" class="modal" aria-hidden="true">
        <div class="modal-content" role="dialog" aria-modal="true" aria-labelledby="termsTitle">
            <div class="modal-header">
                <h3 id="termsTitle" class="modal-title" data-es="Términos y Condiciones" data-en="Terms & Conditions">Términos y Condiciones</h3>
                <button class="modal-close" type="button" data-modal-close aria-label="Close">
                    ✕
                </button>
            </div>
            <div class="modal-body">
                <p data-es="La información enviada será utilizada únicamente para fines de contacto y seguimiento sobre los servicios de Señor Taxes." data-en="The information submitted will be used only to contact you regarding Señor Taxes services.">La información enviada será utilizada únicamente para fines de contacto y seguimiento sobre los servicios de Señor Taxes.</p>
                <p data-es="El envío del formulario no constituye una relación contractual hasta que exista una propuesta aceptada." data-en="Submitting this form does not create a contractual relationship until a proposal is accepted.">El envío del formulario no constituye una relación contractual hasta que exista una propuesta aceptada.</p>
            </div>
        </div>
    </div>

    <script is:inline>
        // Global variables
        let currentStep = 1;
        let personalInfo = {
            name: '',
            phone: ''
        };
        let currentLanguage = 'es'; // Default to Spanish
        const makeWebhookUrl = 'https://hook.us1.make.com/bwrgtpp293rv8oule729judjjizmcncx';

        // Initialize when DOM is loaded
        document.addEventListener('DOMContentLoaded', function() {
            initializeTheme(); // Changed from initializeDarkMode to default to light
            initializeLanguage();
            setupFormValidation();
            showStep(1);
            initializeModals();
            
            // Setup event listeners
            document.getElementById('contactForm').addEventListener('submit', handleFormSubmit);
            document.getElementById('phone').addEventListener('input', formatPhoneNumber);
            document.querySelector('.dark-mode-toggle').addEventListener('click', toggleDarkMode);
            document.querySelector('.language-toggle').addEventListener('click', toggleLanguage);
        });

        // Handle form submission
        async function handleFormSubmit(e) {
            e.preventDefault();
            
            const name = document.getElementById('fullName').value.trim();
            const phone = document.getElementById('phone').value.trim();
            const consent = document.getElementById('privacyConsent').checked;
            const updates = document.getElementById('updatesConsent').checked;
            clearSubmitError();
            
            // Validate inputs
            if (!validateForm(name, phone, consent)) {
                return;
            }

            if (makeWebhookUrl.includes('REEMPLAZA')) {
                const missingMessages = {
                    es: 'Falta configurar el webhook de Make para poder enviar el formulario.',
                    en: 'The Make webhook is missing. Please configure it to submit the form.'
                };
                showSubmitError(missingMessages[currentLanguage]);
                return;
            }
            
            // Show loading state
            const submitBtn = document.getElementById('submitBtn');
            submitBtn.classList.add('loading');
            submitBtn.disabled = true;
            let successCompleted = false;
            
            try {
                const response = await fetch(makeWebhookUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ name, phone, consent, updates })
                });

                if (response.ok) {
                    // Store submitted data
                    personalInfo.name = name;
                    personalInfo.phone = phone;
                    
                    // Show success UI and keep button disabled/hidden
                    showSuccessUI();
                    successCompleted = true;
                    
                    // Transition after a short delay to thank-you step
                    setTimeout(() => {
                        showStep(2);
                    }, 1200);
                } else {
                    const apiMessages = {
                        es: 'No se pudo enviar en este momento. Revisa la integración de Make.',
                        en: 'We could not submit right now. Please check the Make integration.'
                    };
                    showSubmitError(apiMessages[currentLanguage]);
                }
            } catch (error) {
                console.error('Error submitting form:', error);
                const errorMessages = {
                    es: 'No se pudo conectar con el servicio de envío. Revisa tu conexión y Make.',
                    en: 'Could not reach the delivery service. Check your connection and Make.'
                };
                showSubmitError(errorMessages[currentLanguage]);
            } finally {
                // Remove loading state only if not successful
                if (!successCompleted) {
                    submitBtn.classList.remove('loading');
                    submitBtn.disabled = false;
                }
            }
        }

        function showSuccessUI() {
            const submitBtn = document.getElementById('submitBtn');
            const successDiv = document.getElementById('submitSuccess');
            const successText = document.getElementById('submitSuccessText');
            clearSubmitError();
            const messages = { es: 'Enviado correctamente', en: 'Submitted successfully' };
            successText.textContent = messages[currentLanguage];
            submitBtn.classList.remove('loading');
            submitBtn.disabled = true;
            submitBtn.style.display = 'none';
            successDiv.classList.add('show');
        }

        function showSubmitError(message) {
            const submitError = document.getElementById('submitError');
            const submitErrorText = document.getElementById('submitErrorText');
            submitErrorText.textContent = message;
            submitError.classList.add('show');
        }

        function clearSubmitError() {
            const submitError = document.getElementById('submitError');
            const submitErrorText = document.getElementById('submitErrorText');
            submitErrorText.textContent = '';
            submitError.classList.remove('show');
        }

        // Validate form inputs
        function validateForm(name, phone, consent) {
            let isValid = true;
            
            const messages = {
                es: {
                    nameError: 'Por favor ingresa tu nombre completo',
                    phoneError: 'Por favor ingresa un número de teléfono válido',
                    consentError: 'Debes aceptar los términos y la política para continuar.'
                },
                en: {
                    nameError: 'Please enter your full name',
                    phoneError: 'Please enter a valid phone number',
                    consentError: 'You must accept the terms and privacy policy to continue.'
                }
            };
            
            // Validate name
            if (!name || name.length < 2) {
                showError('nameError', messages[currentLanguage].nameError);
                document.getElementById('fullName').classList.add('error');
                isValid = false;
            } else {
                hideError('nameError');
                document.getElementById('fullName').classList.remove('error');
            }
            
            // Validate phone
            const phoneRegex = /^\(?([0-9]{3})\)?[-. ]?([0-9]{3})[-. ]?([0-9]{4})$/;
            if (!phone || !phoneRegex.test(phone.replace(/\D/g, ''))) {
                showError('phoneError', messages[currentLanguage].phoneError);
                document.getElementById('phone').classList.add('error');
                isValid = false;
            } else {
                hideError('phoneError');
                document.getElementById('phone').classList.remove('error');
            }

            if (!consent) {
                showError('consentError', messages[currentLanguage].consentError);
                isValid = false;
            } else {
                hideError('consentError');
            }
            
            return isValid;
        }

        // Show error message
        function showError(elementId, message) {
            const errorElement = document.getElementById(elementId);
            errorElement.textContent = message;
            errorElement.classList.add('show');
        }

        // Hide error message
        function hideError(elementId) {
            const errorElement = document.getElementById(elementId);
            errorElement.classList.remove('show');
        }

        // Format phone number as user types
        function formatPhoneNumber(e) {
            const input = e.target;
            const value = input.value.replace(/\D/g, '');
            let formattedValue = '';
            
            if (value.length > 0) {
                if (value.length <= 3) {
                    formattedValue = `(${value}`;
                } else if (value.length <= 6) {
                    formattedValue = `(${value.slice(0, 3)}) ${value.slice(3)}`;
                } else {
                    formattedValue = `(${value.slice(0, 3)}) ${value.slice(3, 6)}-${value.slice(6, 10)}`;
                }
            }
            
            input.value = formattedValue;
        }

        // Setup form validation
        function setupFormValidation() {
            const nameInput = document.getElementById('fullName');
            const phoneInput = document.getElementById('phone');
            const consentInput = document.getElementById('privacyConsent');
            const submitBtn = document.getElementById('submitBtn');
            
            function checkFormValidity() {
                const name = nameInput.value.trim();
                const phone = phoneInput.value.trim();
                const consent = consentInput.checked;
                const phoneRegex = /^\(?([0-9]{3})\)?[-. ]?([0-9]{3})[-. ]?([0-9]{4})$/;
                
                const isNameValid = name.length >= 2;
                const isPhoneValid = phoneRegex.test(phone.replace(/\D/g, ''));
                
                if (consent) {
                    hideError('consentError');
                }
                submitBtn.disabled = !(isNameValid && isPhoneValid && consent);
            }
            
            nameInput.addEventListener('input', checkFormValidity);
            phoneInput.addEventListener('input', checkFormValidity);
            consentInput.addEventListener('change', checkFormValidity);
            
            // Initial check
            checkFormValidity();
        }

        function initializeModals() {
            const openButtons = document.querySelectorAll('[data-modal-open]');
            const closeButtons = document.querySelectorAll('[data-modal-close]');
            const modals = document.querySelectorAll('.modal');

            openButtons.forEach(button => {
                button.addEventListener('click', () => {
                    const modalId = button.getAttribute('data-modal-open');
                    const modal = document.getElementById(modalId);
                    if (modal) {
                        modal.classList.add('show');
                    }
                });
            });

            closeButtons.forEach(button => {
                button.addEventListener('click', () => {
                    const modal = button.closest('.modal');
                    if (modal) {
                        modal.classList.remove('show');
                    }
                });
            });

            modals.forEach(modal => {
                modal.addEventListener('click', event => {
                    if (event.target === modal) {
                        modal.classList.remove('show');
                    }
                });
            });

            document.addEventListener('keydown', event => {
                if (event.key === 'Escape') {
                    modals.forEach(modal => modal.classList.remove('show'));
                }
            });
        }

        // Show specific step
        function showStep(stepNumber) {
            // Hide all steps
            document.querySelectorAll('.step').forEach(step => {
                step.classList.remove('active');
            });
            
            // Show current step
            document.getElementById(`step${stepNumber}`).classList.add('active');
            
            // Update progress
            updateProgress(stepNumber);
            
            // Update current step
            currentStep = stepNumber;
            
            // If showing thank you page, populate the information
            if (stepNumber === 2) {
                document.getElementById('submittedName').textContent = personalInfo.name;
                document.getElementById('submittedPhone').textContent = personalInfo.phone;
            }
        }

        // Update progress bar
        function updateProgress(step) {
            const progressFill = document.getElementById('progressFill');
            const progressText = document.getElementById('progressText');
            
            const percentage = (step / 2) * 100;
            progressFill.style.width = `${percentage}%`;
            
            const progressMessages = {
                es: {
                    step1: 'Paso 1 de 2',
                    completed: 'Completado'
                },
                en: {
                    step1: 'Step 1 of 2',
                    completed: 'Completed'
                }
            };
            
            if (step === 1) {
                progressText.textContent = progressMessages[currentLanguage].step1;
            } else {
                progressText.textContent = progressMessages[currentLanguage].completed;
            }
        }

        // Initialize theme (default to light mode)
        function initializeTheme() {
            const savedTheme = localStorage.getItem('theme');
            
            // Default to light mode unless explicitly set to dark
            if (savedTheme === 'dark') {
                document.documentElement.setAttribute('data-theme', 'dark');
                updateThemeIcons(true);
            } else {
                document.documentElement.setAttribute('data-theme', 'light');
                updateThemeIcons(false);
            }
        }

        // Initialize language (default to Spanish)
        function initializeLanguage() {
            const savedLanguage = localStorage.getItem('language');
            currentLanguage = savedLanguage || 'es'; // Default to Spanish
            updateLanguageDisplay();
        }

        // Toggle language between Spanish and English
        function toggleLanguage() {
            currentLanguage = currentLanguage === 'es' ? 'en' : 'es';
            localStorage.setItem('language', currentLanguage);
            updateLanguageDisplay();
            updateProgress(currentStep); // Update progress text
        }

        // Update language display
        function updateLanguageDisplay() {
            const languageToggle = document.getElementById('languageToggle');
            // Show the opposite language (EN when in Spanish, ES when in English)
            languageToggle.textContent = currentLanguage === 'es' ? 'EN' : 'ES';
            
            // Update all elements with data-es and data-en attributes
            document.querySelectorAll('[data-es][data-en]').forEach(element => {
                const text = element.getAttribute(`data-${currentLanguage}`);
                if (text) {
                    element.textContent = text;
                }
            });
        }

        // Toggle dark mode
        function toggleDarkMode() {
            const currentTheme = document.documentElement.getAttribute('data-theme');
            const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
            
            document.documentElement.setAttribute('data-theme', newTheme);
            localStorage.setItem('theme', newTheme);
            updateThemeIcons(newTheme === 'dark');
        }

        // Update theme icons
        function updateThemeIcons(isDark) {
            const sunIcon = document.querySelector('.sun-icon');
            const moonIcon = document.querySelector('.moon-icon');
            
            if (isDark) {
                // In dark mode, show sun icon (to switch to light)
                sunIcon.style.display = 'block';
                moonIcon.style.display = 'none';
            } else {
                // In light mode, show moon icon (to switch to dark)
                sunIcon.style.display = 'none';
                moonIcon.style.display = 'block';
            }
        }
    </script>
</body>
</html>
